algoritmo "testevalor"

var
c: inteiro
v: vetor [1..3] de Inteiro
i: inteiro
inicio
     Para c <- 1 ate 3 faca
     Escreva ("Digite alguma coisa: ")
     Leia (i)
     v[c] <- v[c] + i
     FimPara
     para c <- 1 ate 3 faca
     Escreva (v[c])
     fimpara

fimalgoritmo
